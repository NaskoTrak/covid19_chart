// import React, { useState } from 'react';
// import { Search } from 'semantic-ui-react';
// import styles from '../Style.css';

// const SearchBar = (props) => {

//     const [data, setData] = useState("");

    // const [filterText, setFilterText] = useState('');
    
    // const SearchBar = () => {
	// 	return (
	// 		<div>
	// 			<Search
	// 				type="text"
	// 				autoFocus={true} // Because SearchBar losing focus after typing the first letter
	// 				value={filterText}
	// 				onChange={(e) => setFilterText(e.target.value.toLowerCase())}
	// 			/>
	// 		</div>
	// 	);
	// };



    // return (
    //     <div className="searchBar" >
    //         <Search size="small" input="text" placeholder="Select country" value={data} onChange={e => setData(e.target.value) } />
    //     </div>
    //     // <input type="text" className="searchBarBlqq" value={data} onChange={e => setData(e.target.value)} />
    // );

};

// export default SearchBar;